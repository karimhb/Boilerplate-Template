var React = require('react');

var Main = React.createClass({
    render: function () {
      return (
        <div>
          <h2>Main component</h2>
        </div>
    );
    } 
 });

module.exports = Main;
